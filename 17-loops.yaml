- name: loops
  hosts: local
  connection: local
  tasks:
    - name: Print Names
      ansible.builtin.debug:
        msg: "{{ item }}"  # Here item is the reserved keyword for loop iteration
      loop:
      - "Siva"
      - "Siva 2"
      - "Siva 3"